"""CrewAI Orchestrator — agent-based development pipeline for Drake portfolio.

Package layout (to be filled by subsequent slices):
    agents/     — PO, EL, RM agent definitions
    crews/      — refinement, audit, sync crew compositions
    tools/      — shared CrewAI tools (git, gh, file, cockpit, validation)
    tasks/      — reusable task templates
"""

__version__ = "0.1.0"
